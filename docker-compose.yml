version:'3'
services:
  redis:
    images: redis:alpine
    container_name:redis
    ports:
      - "6379:6379"

  app:
    build: .
    container_name: go-redis-app
    depends_on:
      - redis
    ports:
      -"8080:8080"
